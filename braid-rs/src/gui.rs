// Minimal egui scaffold. This is an integration point for a
// retro-themed dashboard (drag-and-drop ROMs, session status, logs).

#[allow(dead_code)]
pub fn run_gui() {
    // Placeholder: actual integration would use eframe or another windowing
    // shell to host an egui app. This function exists so the crate already
    // has a dedicated module for GUI work.
}
